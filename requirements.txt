pandas==3.0.5
numpy==2.5.1
pyarrow==25.0.0
fastparquet==2026.5.0
appdirs==1.4.4
requests==2.34.2
pyyaml==6.0.2
xgboost==3.4.0
scikit-learn==1.9.0
pytest==8.3.4
streamlit==1.61.0
# pinned: streamlit 1.61.0's bundled gzip middleware breaks against
# starlette's latest 1.x (GZipResponder gained a required kwarg it doesn't
# pass) -- an unpinned install resolves the incompatible latest version.
starlette==0.47.2
