pandas==3.0.5
numpy==2.5.1
# pinned <25: streamlit 1.61.0 (pinned below) requires pyarrow<25, so
# pyarrow==25.0.0 made this file internally unsatisfiable -- scripts/install.sh
# failed with ResolutionImpossible on every Python version. 24.0.0 is the
# newest release satisfying streamlit; pandas only needs >=13.
pyarrow==24.0.0
fastparquet==2026.5.0
appdirs==1.4.4
requests==2.34.2
pyyaml==6.0.2
xgboost==3.4.0
scikit-learn==1.9.0
pytest==8.3.4
streamlit==1.61.0
# pinned: streamlit 1.61.0's bundled gzip middleware breaks against
# starlette's latest 1.x (GZipResponder gained a required kwarg it doesn't
# pass) -- an unpinned install resolves the incompatible latest version.
starlette==0.47.2
espn_api==0.46.0
python-dotenv==1.2.2
